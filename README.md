# Шаблон проекта для написания автотестов

Каркас для создания автотестов.

Есть пример класса `App\Math`, покрытый автотестом `Tests\Unit\MathTest`.

Для запуска тестов есть команда `cmd/test/test.bat`.